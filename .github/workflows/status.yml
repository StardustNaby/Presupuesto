name: Status Check

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]

jobs:
  status:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Verify project structure
      run: |
        echo "✅ Verificando estructura del proyecto..."
        test -f "PresupuestoFamiliarMensual.sln" || exit 1
        test -f "Dockerfile" || exit 1
        test -f "railway.json" || exit 1
        echo "✅ Estructura del proyecto correcta"
        
    - name: Verify API project
      run: |
        echo "✅ Verificando proyecto API..."
        test -f "src/PresupuestoFamiliarMensual.API/PresupuestoFamiliarMensual.API.csproj" || exit 1
        echo "✅ Proyecto API encontrado"
        
    - name: Success
      run: |
        echo "🎉 Todas las verificaciones pasaron exitosamente!"
        echo "✅ El proyecto está listo para desplegar" 